# Setup на нов компютър за DSHOME проект

**Забележка:** Преди да започнеш, убеди се че имаш инсталирани:
- Node.js >= 20.0.0
- pnpm >= 8.0.0
- Docker Desktop
- Git

Проверка на версиите:
```bash
node --version    # трябва да е v20.x или по-ново
pnpm --version    # трябва да е 8.x или по-ново
docker --version  # трябва да работи
git --version     # трябва да работи
```

---

## Стъпка 1: Clone проекта

```bash
# В Git Bash или PowerShell
cd D:\  # или друга директория където искаш
git clone https://github.com/dshomebg/dshome-docker.git 001-DSHOME-DOCKER
cd 001-DSHOME-DOCKER
```

**Важно:** Директорията се казва `001-DSHOME-DOCKER`, не `dshome-docker`!

---

## Стъпка 2: Инсталирай dependencies

```bash
pnpm install
```

Това ще инсталира зависимостите за всички packages (shared, backend, admin).

---

## Стъпка 3: Конфигурирай .env файла

```bash
# Копирай примерния .env файл
copy .env.example .env
```

Файлът `.env` вече съдържа правилни настройки за development. **Не е нужно да го променяш** освен ако не искаш да използваш други портове.

**Какво съдържа .env:**
- Database: PostgreSQL на localhost:5432
- Redis: localhost:6379
- Meilisearch: localhost:7700
- Backend: порт 4000
- Admin: порт 3001

---

## Стъпка 4: Стартирай Docker контейнерите

```bash
# Стартирай PostgreSQL, Redis и Meilisearch
pnpm docker:dev:up

# Провери дали работят (трябва да видиш 3 контейнера)
docker ps
```

Очаквани контейнери:
- `dshome-postgres-dev` → порт 5432
- `dshome-redis-dev` → порт 6379
- `dshome-meilisearch-dev` → порт 7700

---

## Стъпка 5: Setup на базата данни

```bash
# Прилагане на миграции
pnpm db:migrate

# Зареждане на примерни данни (admin потребител и др.)
pnpm db:seed
```

**Seed данни:**
- Admin потребител: `admin@dshome.dev` / `admin123`
- Примерни категории и складове

---

## Стъпка 6: Стартирай development сървърите

**Вариант 1 - Всичко наведнъж (препоръчително):**
```bash
pnpm dev
```

**Вариант 2 - Отделни терминали:**
```bash
# Терминал 1 - Backend
pnpm --filter @dshome/backend dev

# Терминал 2 - Admin
pnpm --filter @dshome/admin dev
```

---

## Стъпка 7: Отвори приложенията

**Backend API:**
- URL: http://localhost:4000/api/health

**Admin Panel:**
- URL: http://localhost:3001/admin/
- Login: `admin@dshome.dev` / `admin123`

**Database GUI (Drizzle Studio):**
```bash
pnpm db:studio
```
Отваря се на https://local.drizzle.studio

---

**Готово!** Сега можеш да работиш от новия компютър.

---

## Синхронизация на работата между компютрите

### Когато ЗАВЪРШВАШ работа (преди да затвориш компютъра):

```bash
# 1. Провери какво си променил
git status

# 2. Добави и commit-ни промените
git add .
git commit -m "Описание на промените"

# 3. Push-ни към GitHub
git push
```

### Когато ЗАПОЧВАШ работа на другия компютър:

```bash
# 1. Pull последните промени от GitHub
git pull

# 2. Инсталирай нови dependencies (ако има такива)
pnpm install

# 3. Провери дали Docker контейнерите работят
docker ps

# Ако не работят:
pnpm docker:dev:up

# 4. Стартирай dev сървърите
pnpm dev
```

---

## Важни правила при работа с 2 компютъра

### ✅ ДОБРЕ - Този workflow е стандартен и безопасен:
- Git автоматично синхронизира кода
- Всичко е в GitHub - няма загуба на код
- Използва се от всички разработчици по света

### ⚠️ ВАЖНИ ПРАВИЛА:

1. **ВИНАГИ commit & push преди да затвориш компютъра**
   - Иначе промените няма да се синхронизират

2. **ВИНАГИ pull преди да започнеш работа**
   - Иначе ще работиш със стар код

3. **НЕ работи едновременно от двата компютъра върху същите файлове**
   - Ще има merge конфликти

4. **НЕ commit-вай .env файла**
   - Вече е в .gitignore
   - Копирай го ръчно между компютрите (само веднъж при първоначален setup)

### 🔥 Ако забравиш да push и имаш конфликти:

```bash
# Ще видиш грешка при git pull
git pull

# Провери кои файлове са в конфликт
git status

# Отвори конфликтните файлове в редактор и оправи конфликтите
# (VS Code има визуален инструмент за merge conflicts)

# След като оправиш конфликтите:
git add .
git commit -m "Resolve merge conflicts"
git push
```

---

## Бърза справка - Често използвани команди

| Действие | Команда |
|----------|---------|
| Започвам работа | `git pull` → `pnpm docker:dev:up` → `pnpm dev` |
| Завършвам работа | `git add .` → `git commit -m "message"` → `git push` |
| Виж промени | `git status` |
| Отмени промени | `git restore <file>` |
| Стартирай Docker | `pnpm docker:dev:up` |
| Спри Docker | `pnpm docker:dev:down` |
| Database migrations | `pnpm db:migrate` |
| Database GUI | `pnpm db:studio` |
| Seed database | `pnpm db:seed` |

---

## Troubleshooting - Често срещани проблеми

### 1. "Cannot connect to database"

```bash
# Провери дали PostgreSQL контейнерът работи
docker ps | grep postgres

# Ако не работи, стартирай го
pnpm docker:dev:up
```

### 2. "Port already in use" (4000 или 3001)

```bash
# Намери процеса който използва порта
netstat -ano | findstr :4000

# Спри процеса (замени <PID> с реалния номер)
cmd /c taskkill /PID <PID> /F
```

### 3. "Module not found" грешки

```bash
# Инсталирай dependencies
pnpm install

# Ако не помогне, изчисти кеша
pnpm store prune
rm -rf node_modules
pnpm install
```

### 4. Docker контейнерите не стартират

```bash
# Спри всичко
pnpm docker:dev:down

# Провери дали Docker Desktop работи
docker ps

# Стартирай отново
pnpm docker:dev:up
```

### 5. Database migrations не работят

```bash
# Провери дали базата е достъпна
docker ps | grep postgres

# Стартирай migrations
pnpm db:migrate

# Ако има грешка, провери .env файла
```

---

## Препоръки за добър workflow

1. **Commit-вай често** - на всеки 30-60 мин, не чакай до края на деня
2. **Пиши ясни commit messages** - "Fix category form validation" вместо "fixes"
3. **Pull преди push** - винаги `git pull` преди `git push` за да избегнеш конфликти
4. **Проверявай Docker преди работа** - `docker ps` преди да стартираш `pnpm dev`

---

**Последна актуализация:** 2025-11-11
