# Setup на нов компютър за DSHOME проект

## Стъпка 1: Инсталирай софтуер

### Windows:
```powershell
# 1. Node.js 20 LTS
# Свали от: https://nodejs.org/ (LTS версия)

# 2. Git
# Свали от: https://git-scm.com/download/win

# 3. pnpm (след като инсталираш Node.js)
npm install -g pnpm

# 4. Docker Desktop (опционално - само ако искаш да тестваш production build локално)
# Свали от: https://www.docker.com/products/docker-desktop/
```

## Стъпка 2: Clone проекта

```bash
# В Git Bash или PowerShell
cd D:\  # или друга директория където искаш
git clone https://github.com/dshomebg/dshome-docker.git
cd dshome-docker
```

## Стъпка 3: Setup на проекта

```bash
# Инсталирай dependencies
pnpm install

# Копирай .env файла
# ВАЖНО: Попитай колегите за .env файла или го копирай от другия компютър
# Файлът трябва да съдържа DATABASE_URL, API ключове и др.
```

## Стъпка 4: Стартирай development сървърите

```bash
# В един терминал - Backend
cd packages/backend
pnpm dev

# В друг терминал - Admin
cd packages/admin
pnpm dev
```

**Готово!** Сега можеш да работиш от новия компютър.

---

## Синхронизация на работата между компютрите

### Когато ЗАВЪРШВАШ работа (преди да затвориш компютъра):

```bash
# 1. Провери какво си променил
git status

# 2. Commit-ни промените
git add .
git commit -m "Описание на промените"

# 3. Push-ни към GitHub
git push
```

### Когато ЗАПОЧВАШ работа (след като отвориш компютъра):

```bash
# 1. Pull последните промени от GitHub
git pull

# 2. Инсталирай нови dependencies (ако има)
pnpm install

# 3. Стартирай dev сървърите
# Backend: cd packages/backend && pnpm dev
# Admin: cd packages/admin && pnpm dev
```

---

## Проблемен ли е този начин на работа?

### ✅ ДОБРЕ:
- **Лесно**: Git автоматично синхронизира кода
- **Безопасно**: Всичко е в GitHub - няма загуба на код
- **Стандартно**: Точно така работят всички разработчици

### ⚠️ ВНИМАНИЕ:
1. **ВИНАГИ commit & push преди да затвориш компютъра**
   - Иначе промените няма да се синхронизират

2. **ВИНАГИ pull преди да започнеш работа**
   - Иначе може да имаш стар код

3. **НЕ работи едновременно от двата компютъра**
   - Ще има merge конфликти

4. **НЕ commit-вай .env файла**
   - Вече е в .gitignore, но внимавай
   - Копирай го ръчно между компютрите (веднъж)

### 🔥 Ако забравиш да push и имаш различни промени на двата компютъра:

```bash
# На компютъра където имаш по-новите промени:
git pull  # Ще каже че има конфликт
git status  # Вижи кои файлове са в конфликт

# Отвори файловете и избери коя версия искаш да запазиш
# После:
git add .
git commit -m "Merge changes from both computers"
git push
```

---

## Бърза справка

| Действие | Команда |
|----------|---------|
| Започвам работа | `git pull` → `pnpm install` → `pnpm dev` |
| Завършвам работа | `git add .` → `git commit -m "message"` → `git push` |
| Виж промени | `git status` |
| Отмени промени | `git restore <file>` |
| Deploy на production | `ssh root@157.90.129.12 "cd /opt/dshome && git pull && docker compose -f docker-compose.prod.yml build --no-cache && docker compose -f docker-compose.prod.yml up -d"` |

---

## Препоръки

1. **Commit-вай често** - всеки 30-60 мин, не чакай до края на деня
2. **Пиши ясни commit messages** - "Fix category form validation" вместо "fixes"
3. **Pull преди push** - винаги `git pull` преди `git push` за да избегнеш конфликти
4. **Използвай branches** за големи features (опционално):
   ```bash
   git checkout -b feature/new-feature
   # работиш...
   git push -u origin feature/new-feature
   ```

**Заключение**: Този начин на работа е **перфектно нормален и безопасен**, стига да спазваш правилото: **commit & push в края, pull в началото**.
